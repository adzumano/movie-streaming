import classNames from 'classnames';
import React from 'react';

import styles from './MovieCard.module.scss';

export const MovieCard = () => {
    return (
        <div>
            <p className={classNames(styles.card, 'text-4xl')}>dsadadsad</p>
        </div>
    );
};
